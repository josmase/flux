apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/gitlab
  - ../../../base/gitlab-runners
  - ../../gitlab
  - ../../gitlab-runners
  - redis-canonical.yaml
  - postgresql-canonical.yaml

components:
  - ../../components/prune-protection

patches:
  - path: redis-canonical-patch.yaml
    target:
      group: helm.toolkit.fluxcd.io
      version: v2
      kind: HelmRelease
      name: gitlab
      namespace: gitlab
  - path: postgresql-canonical-patch.yaml
    target:
      group: helm.toolkit.fluxcd.io
      version: v2
      kind: HelmRelease
      name: gitlab
      namespace: gitlab
  - target:
      group: helm.toolkit.fluxcd.io
      version: v2
      kind: HelmRelease
      name: gitlab-runner
      namespace: gitlab-runners
    patch: |-
      - op: add
        path: /spec/valuesFrom
        value:
          - kind: Secret
            name: gitlab-runner-token
            valuesKey: runner-token
            targetPath: runnerToken
