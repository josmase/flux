apiVersion: apps/v1
kind: Deployment
metadata:
  name: gotify
spec:
  template:
    spec:
      volumes:
        - name: gotify-data
          persistentVolumeClaim:
            claimName: gotify-data-final
