apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bazarr-shared-storage-pvc
spec:
  storageClassName: shared-nfs-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 110Ti