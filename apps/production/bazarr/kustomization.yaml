apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - ingress.yaml
  - persistence.yaml
  - service.yaml

labels:
  - includeSelectors: true
    pairs:
      app: bazarr
