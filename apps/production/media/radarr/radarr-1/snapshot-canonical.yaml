apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: data-to-canonical
  namespace: media
spec:
  volumeSnapshotClassName: linstor-snapshot
  source:
    persistentVolumeClaimName: radarr-1-config-triple
