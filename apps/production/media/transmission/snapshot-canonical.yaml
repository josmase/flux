apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: transmission-data-to-canonical
spec:
  volumeSnapshotClassName: linstor-snapshot
  source:
    persistentVolumeClaimName: transmission-config-triple
