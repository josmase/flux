apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-linstor
  annotations:
    storage-migration.source-pvc: sonarr-6-config-resized
    storage-migration.phase: copy-target
  labels:
    app: sonarr-6
    storage-migration: sonarr-6-linstor-final-2026-09
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: linstor-final
  resources: {requests: {storage: 2Gi}}
