apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: config-to-canonical
spec:
  volumeSnapshotClassName: linstor-snapshot
  source:
    persistentVolumeClaimName: sonarr-5-config-triple
