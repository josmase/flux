apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: container-registry-internal-pvc
spec:
  storageClassName: app-nfs-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi