apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: container-registry-internal-pvc
spec:
  storageClassName: nfs-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi