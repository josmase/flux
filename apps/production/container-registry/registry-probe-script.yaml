apiVersion: v1
kind: ConfigMap
metadata:
  name: registry-probe-script
data:
  probe.sh: |
    #!/bin/sh
    code=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:5000$1)
    if [ $code -eq 200 ] || [ $code -eq 401 ]; then
      exit 0
    else
      exit 1
    fi
