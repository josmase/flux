apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ingress.yaml
  - service.yaml
  - stateful-set.yaml

configMapGenerator:
  - name: registry-config
    files:
      - config.yaml