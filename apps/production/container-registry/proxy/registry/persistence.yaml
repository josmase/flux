apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: container-registry-proxy-pvc
spec:
  storageClassName: app-nfs-storage
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi