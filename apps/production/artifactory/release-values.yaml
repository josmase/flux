databaseUpgradeReady: true

artifactory:
  replicaCount: 3
  masterKeySecretName: my-masterkey-secret
  joinKeySecretName: my-joinkey-secret
  persistence:
    enabled: true
    existingClaim: shared-nfs-pvc

nginx:
  enabled: false

postgresql:
  postgresqlPassword: somepassword
  persistence:
    enabled: true
    size: 50Gi