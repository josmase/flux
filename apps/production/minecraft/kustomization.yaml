apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: minecraft

resources:
  - ../../base/minecraft
  - secret.yaml
  - snapshot-canonical.yaml
  - persistence-canonical.yaml

patches:
  - path: deployment-canonical.yaml
