apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shared-nfs-pvc
spec:
  storageClassName: "manual"
  volumeName: shared-nfs-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 110Ti
