apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Development persistence overlay
# Replaces NFS PersistentVolume with hostPath for local Kind clusters

resources:
  - storage-class.yaml
  - default
  - immich
