apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default

# NOTE: Environment overlays must provide:
# - secret.yaml (containing mongo-express-password with ME_CONFIG_BASICAUTH_PASSWORD)

resources:
- deployment.yaml
- service.yaml
- ingress.yaml