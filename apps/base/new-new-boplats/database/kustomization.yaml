apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# NOTE: Environment overlays must provide:
# - secret.yaml (containing db-passwords with 3 database passwords)

resources:
  - database.yaml
  - admin
