apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  # Replacement PVC only. The Deployment remains on the source claim until
  # the documented backup/copy/validation cutover is completed.
  - persistence-resized.yaml
  - ingress.yaml
  - service.yaml

labels:
  - includeSelectors: true
    pairs:
      app: bazarr
