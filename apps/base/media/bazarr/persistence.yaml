apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: bazarr-config-pvc-bazarr-0
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ${STORAGE_CLASS}
  selector:
    matchLabels:
      media-app: bazarr
  resources:
    requests:
      storage: 5Gi
