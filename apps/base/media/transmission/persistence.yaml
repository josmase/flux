apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: transmission-config-pvc-transmission-0
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ${STORAGE_CLASS}
  selector:
    matchLabels:
      media-app: transmission
  resources:
    requests:
      storage: 1Gi
