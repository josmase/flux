apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: transmission-config-pvc-transmission-0
  annotations:
    kustomize.toolkit.fluxcd.io/ssa: merge
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ${STORAGE_CLASS}
  resources:
    requests:
      storage: 1Gi
