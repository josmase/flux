apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: media

resources:
  - namespace.yaml
  - bazarr
  - seerr
  - prowlarr
  # Unified dashboard for all instances
  - arr-dashboard
  # Transmission is enabled by environment overlays because its persistence
  # class and MetalLB address are environment-specific.
  - jellyfin
  - checkrr
  - reiverr
