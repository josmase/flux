apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: reiverr

spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`reiverr.${DOMAIN_INTERNAL}`)
      kind: Rule
      services:
        - name: reiverr
          port: http

  tls:
    secretName: ${CERT_SECRET_INTERNAL}
