apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - snapshot-canonical.yaml
  - persistence-canonical.yaml
  - persistence.yaml
  - ingress.yaml
  - service.yaml
  - log-watcher-rbac.yaml

labels:
  - includeSelectors: true
    pairs:
      app: jellyfin
