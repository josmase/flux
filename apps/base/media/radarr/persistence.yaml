apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radarr-config-pvc-radarr-0
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ${STORAGE_CLASS}
  selector:
    matchLabels:
      media-app: radarr
  resources:
    requests:
      storage: 30Gi
