apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: radarr
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`radarr.${DOMAIN_INTERNAL}`)
      kind: Rule
      services:
        - name: radarr
          port: http

  tls:
    secretName: ${CERT_SECRET_INTERNAL}
