apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-config-pvc-plex-0
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ${STORAGE_CLASS}
  resources:
    requests:
      storage: 50Gi
