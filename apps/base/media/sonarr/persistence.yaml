apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-config-pvc-sonarr-0
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ${STORAGE_CLASS}
  resources:
    requests:
      storage: 10Gi
