apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-config-pvc-sonarr-0
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ${STORAGE_CLASS}
  selector:
    matchLabels:
      media-app: sonarr
  resources:
    requests:
      storage: 10Gi
