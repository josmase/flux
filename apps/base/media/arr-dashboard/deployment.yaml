apiVersion: apps/v1
kind: Deployment
metadata:
  name: arr-dashboard
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: arr-dashboard
  template:
    metadata:
      labels:
        app: arr-dashboard
    spec:
      containers:
        - name: arr-dashboard
          image: artifactory.local.hejsan.xyz/docker/khak1s/arr-dashboard:2.13.0@sha256:3ddcc61d9d1abcc5b42362c770bdcfd5dd7bfc2d6ae3ad8bf5033f0078980dfe
          ports:
            - containerPort: 3000
              name: http
          envFrom:
            - configMapRef:
                name: default-app-config
          livenessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2
          volumeMounts:
            - name: config
              mountPath: /config
          resources:
            limits:
              memory: 512Mi
              cpu: "0.5"
            requests:
              memory: 128Mi
              cpu: "0.1"
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: arr-dashboard-config
