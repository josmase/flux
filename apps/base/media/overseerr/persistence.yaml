apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: overseerr-config-pvc-overseerr-0
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ${STORAGE_CLASS}
  resources:
    requests:
      storage: 2Gi
