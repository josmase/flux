apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app: container-registry-internal-ui

resources:
  - configmap.yaml
  - deployment.yaml
  - ingress.yaml
  - service.yaml