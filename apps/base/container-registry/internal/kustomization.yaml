apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app: container-registry-internal

resources:
  - configmap.yaml
  - password.yaml
  - deployment.yaml
  - ingress.yaml
  - service.yaml
  - persistence.yaml
  - secret.yaml