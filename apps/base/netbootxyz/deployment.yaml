apiVersion: apps/v1
kind: Deployment
metadata:
  name: netbootxyz
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: netbootxyz
  template:
    metadata:
      labels:
        app: netbootxyz
    spec:
      initContainers:
        - name: configure-live-endpoint
          image: busybox:1.37@sha256:1487d0af5f52b4ba31c7e465126ee2123fe3f2305d638e7827681e7cf6c83d5e
          command:
            - sh
            - -c
            - |
              set -euo pipefail
              cat > /config/local-vars.ipxe <<'EOF'
              # auto-generated to point assets to local mirror
              set live_endpoint ${LIVE_ENDPOINT}
              EOF
          env:
            - name: LIVE_ENDPOINT
              value: "http://netboot-assets.${DOMAIN_INTERNAL}"
          volumeMounts:
            - name: config
              mountPath: /config
      containers:
        - name: netbootxyz
          image: artifactory.local.hejsan.xyz/docker/netbootxyz/netbootxyz:latest@sha256:e95b81cebf3af6f7223666289ff3841c607b83914a6c13a9c05fd6b7524ab190
          ports:
            - containerPort: 3000
              name: web
              protocol: TCP
            - containerPort: 69
              name: tftp
              protocol: UDP
            - containerPort: 80
              name: assets
              protocol: TCP
          env:
            - name: MENU_VERSION
              value: "2.0.84"
            - name: NGINX_PORT
              value: "80"
            - name: WEB_APP_PORT
              value: "3000"
            - name: LIVE_ENDPOINT
              value: "http://netboot-assets.${DOMAIN_INTERNAL}"
          volumeMounts:
            - name: config
              mountPath: /config
            - name: assets
              mountPath: /assets
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: netbootxyz-config-pvc
        - name: assets
          persistentVolumeClaim:
            claimName: netbootxyz-assets-pvc
