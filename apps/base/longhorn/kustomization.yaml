apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: longhorn-system
resources:
  - ingress.yaml
  #- longhorn-maintenance-cronjob.yaml
  # The production-only SSH Secret lives under apps/production/longhorn/ and
  # must be composed with this workload if maintenance is re-enabled.
