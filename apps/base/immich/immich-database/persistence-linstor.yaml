apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-db-triple
  labels:
    app.kubernetes.io/part-of: immich
    migration.josmase.dev/source: immich-db-pvc-final
    migration.josmase.dev/phase: copy-target
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: linstor-final-triple
  resources:
    requests:
      storage: 9Gi
