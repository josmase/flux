apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: llm-switchboard-data
  labels:
    app: llm-switchboard
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ${STORAGE_CLASS}
  resources:
    requests:
      storage: 1Gi
