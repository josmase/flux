apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# NOTE: Environment overlays must provide:
# - secret-runner-token.yaml (containing github-runner-token with github_token)

resources:
  - source.yaml
  - namespace.yaml
