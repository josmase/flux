apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: grafana-data-to-canonical
  namespace: monitoring
spec:
  volumeSnapshotClassName: linstor-snapshot
  source:
    persistentVolumeClaimName: grafana-data-triple
