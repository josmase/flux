apiVersion: apps/v1
kind: Deployment
metadata:
  name: pterodactyl-panel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pterodactyl-panel
  template:
    metadata:
      labels:
        app: pterodactyl-panel
    spec:
      containers:
        - name: panel
          image: ghcr.io/pterodactyl/panel:1.11.4
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
              name: http
          env:
            - name: APP_URL
              value: "https://pterodactyl.${DOMAIN_INTERNAL}"
            - name: APP_ENV
              value: "production"
            - name: CACHE_DRIVER
              value: "redis"
            - name: SESSION_DRIVER
              value: "redis"
            - name: QUEUE_CONNECTION
              value: "redis"
            - name: DB_CONNECTION
              value: "pgsql"
            - name: DB_HOST
              value: "pterodactyl-postgres"
            - name: DB_PORT
              value: "5432"
            - name: REDIS_HOST
              value: "pterodactyl-redis"
            - name: REDIS_PORT
              value: "6379"
          envFrom:
            - configMapRef:
                name: default-app-config
            - secretRef:
                name: pterodactyl-secrets
          volumeMounts:
            - name: panel-storage
              mountPath: /var/www/html
      volumes:
        - name: panel-storage
          persistentVolumeClaim:
            claimName: pterodactyl-panel-pvc
