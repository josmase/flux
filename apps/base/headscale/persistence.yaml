apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: headscale-data-pvc-headscale-0
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ${STORAGE_CLASS}
  resources:
    requests:
      storage: 20Gi
