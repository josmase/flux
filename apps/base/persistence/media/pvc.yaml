apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-shared-nfs-pvc
  namespace: media
spec:
  storageClassName: "manual"
  volumeName: media-shared-nfs-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 110Ti
