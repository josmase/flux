apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: immich-shared-nfs-pvc
spec:
  storageClassName: "manual"
  volumeName: immich-shared-nfs-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 110Ti
