apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: node-red-data-pvc-node-red-0
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ${STORAGE_CLASS}
  resources:
    requests:
      storage: 1Gi
