---
# RuntimeClass for NVIDIA GPU workloads
# Enables pods to use the nvidia-container-runtime for GPU acceleration
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nvidia
handler: nvidia
