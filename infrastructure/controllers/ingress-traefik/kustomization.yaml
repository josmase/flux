apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - release.yaml
  - source.yaml
configMapGenerator:
  - name: traefik-values
    files:
      - values.yaml=my-values.yaml
configurations:
  - kustomizeconfig.yaml
